#include<vector>
using namespace std;
class Solution {
public:

    int searchAns(vector<int>& nums,int target)
    {
        int left = 0;
        int right = 0;
        int result = 0;
        int sum = 0;
        while (right<nums.size())
        {
            if (sum<=target)
            { 
                ++right;

            }
            
        }
        
        
    }
    vector<int> answerQueries(vector<int>& nums, vector<int>& queries) {
        for (size_t i = 0; i < queries.size(); i++)
        {

        }
        
    }
};